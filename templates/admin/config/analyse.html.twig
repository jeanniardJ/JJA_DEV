{% extends 'admin/config/_body.html.twig' %}
{% set navTabActive = 'analyse' %}
{% block tabContent %}
    <div class="tab-pane fade show active" id="analyse" role="tabpanel" aria-labelledby="analyse-tab">
        {{ form_start(formAnalyse) }}
        {{ form_widget(formAnalyse) }}
        {{ form_rest(formAnalyse) }}
        {{ form_end(formAnalyse) }}
    </div>
{% endblock %}

